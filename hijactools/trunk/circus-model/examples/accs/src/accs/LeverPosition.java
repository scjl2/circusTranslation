package accs;

/* Possible inputs values for the lever. */

/* This enumeration corresponds to the LEVER type in the Circus model. */

public enum LeverPosition {
   ACTIVATE,
   DEACTIVATE,
   START_ACCELERATION,
   STOP_ACCELERATION,
   RESUME
}
